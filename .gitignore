# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch artifacts (logo scans, scraped pages, etc.)
.tmp/

# Local Azure deployment artifacts (do not commit)
publishProfile.xml
azure-setup-script.sh

# Cursor workspace images
assets/c__Users_*

# Backup files (local only - not committed)
server/backups/*.json

# Backend build stamp (generated in CI)
server/buildInfo.generated.json

# Embedded repositories (should be separate)
OpensDoorsV2/

# Local commit helper files (never commit these)
_commit_msg.txt
_commit_msg*.txt
