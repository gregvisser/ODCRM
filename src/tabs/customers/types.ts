// Customers tab-local scaffolding types.
// Shared / cross-tab types must be requested via Sync/Integrity Agent and live under `src/contracts/**` or `src/types/**`.

export type CustomerId = string


