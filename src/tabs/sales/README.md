### Sales tab (OpenDoors Sales)

- **Owner**: Sales Agent
- **Scope (now)**: placeholder UI + tab-local scaffolding only
- **Non-goals (now)**: no pipeline logic, no CRM automation, no backend calls, no shared type edits


