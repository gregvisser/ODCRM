### Onboarding tab (Onboarding)

- **Owner**: Onboarding Agent
- **Live subnav scope (now)**:
  - `customer-onboarding` (default view when a client is selected)
  - `progress-tracker` (safe fallback when no client is selected)
- **Legacy status**: `OnboardingOverview.tsx` still exists in code for compatibility but is not part of the live onboarding subnav flow.
- **Non-goals (now)**: no workflow engine, no scheduling integrations, no shared type edits


